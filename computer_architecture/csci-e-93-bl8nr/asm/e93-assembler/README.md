A bare bones starting point for an assembler.

Build as follows:

```
$ ./mvnw clean install
```

Running:

- build as shown above
- java -jar target/e93-assembler-1.0-SNAPSHOT.jar name-of-your-file.asm
- ./mvnw clean test -Dtest=AssemblerTest

